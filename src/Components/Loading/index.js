import Spinner from "../Spinner";

const index = () => {
  return (
    <div className="flex items-center justify-center text-green h-[50vh] w-full">
      <Spinner />
    </div>
  );
};

export default index;
